# v1.2.1 Personal Project Previews Worklog

## Summary

Connected the supplied Wizard Defense and Inquiry Dataset screenshots to the existing project preview system without changing card behavior or project actions.

## Implementation

- Changed `wizard-defense` from an abstract preview to `assets/projects/wizard-defense/preview.png`.
- Changed `inquiry-dataset` from an abstract preview to `assets/projects/inquiry-dataset/preview.png`.
- Added the requested descriptive alt text to both previews.
- Kept the existing `import.meta.env.BASE_URL` asset resolution helper unchanged.

## Preserved behavior

- ProjectCard hover preview activation and image zoom.
- ProjectThumbnail `onError` transition to the abstract gradient fallback.
- Light/dark styling and KO/EN language state.
- Team Projects / Personal Projects grouping.
- STACCATO and 404 R·N·F AI previews and live-site actions.
- No live-site actions for Wizard Defense or Inquiry Dataset.

## Protected scope

- No dependency installation.
- No package manifest, Vite, GitHub Pages, workflow, or Resume file changes.

## Verification

- `npm.cmd run lint`: passed
- `npm.cmd run build`: passed; Vite transformed 107 modules
- `git diff --check`: passed

## Remaining issues

- Manual light/dark and hover inspection remains recommended before merge.
