# v0.12 Resume Content Slots Worklog

## 작업 날짜

- 2026-07-09

## 브랜치

- feature/v0.12-resume-content-slots

## 작업 목표

- Resume 페이지에 실제 증명사진과 수료증 PDF를 연결한다.
- 이력서 내용을 `src/data/resume.ts`로 분리해 나중에 쉽게 수정할 수 있게 정리한다.
- 실제 브라우저 업로드 기능은 만들지 않고, public 정적 자산만 연결한다.

## 확인한 파일

- `public/assets/resume/profile/profile-photo.jpg`: 존재
- `public/assets/resume/certificates/certificate-01.pdf`: 존재
- `public/assets/resume/certificates/certificate-02.pdf`: 없음

## 변경 내용

- `src/data/resume.ts`
  - 기본 프로필 정보, 증명사진 정보, 희망 직무, 연락처 링크, 기술 역량, 교육 이력, 수료증 목록, 자격증 목록, 자기소개 요약/상세, 프로젝트 경험 요약을 데이터로 분리했다.
  - 수료증 1은 PDF 링크를 연결했다.
  - 수료증 2는 실제 파일이 없어 `planned` 상태와 `파일 추가 예정` 표시로 처리했다.
- `src/pages/Resume.tsx`
  - 하드코딩된 이력서 데이터를 제거하고 `resumeContent`를 import해 렌더링하도록 정리했다.
  - 증명사진 경로가 있으면 `<img>`로 표시하고, 3:4 비율과 권장 크기 안내를 유지했다.
  - 수료증 `fileUrl`이 있으면 새 탭으로 열리는 `수료증 보기` 버튼을 표시한다.
  - 수료증 `fileUrl`이 없으면 `파일 추가 예정` 상태로 표시한다.
  - 자기소개 요약 / 자세히 보기 토글 구조를 유지했다.
- `docs/VERSION_PLAN.md`
  - v0.12 Resume Content Slots 계획을 추가했다.

## 개인정보 / 민감정보 주의사항

- `public/assets/resume` 안의 파일은 배포 후 공개될 수 있다.
- 증명사진과 수료증은 URL을 아는 사람이 접근할 수 있다.
- 수료증에 주민등록번호, 생년월일, 주소, 전화번호, 교육생 번호 등 민감정보가 있으면 반드시 가리고 업로드해야 한다.
- 실제 이메일, 전화번호, 학력, 자격증 내용은 임의로 만들지 않았다.

## 검증 결과

- lint: `npm.cmd run lint` 통과
- build: `npm.cmd run build` 통과

## 남은 이슈

- 브라우저 수동 확인은 별도로 수행하지 않았다.
- 실제 이메일, 전화번호, 학력, 자격증 정보는 확정 후 추가해야 한다.
- `certificate-02.pdf`는 아직 파일이 없어 추가 예정 상태로 남겨두었다.

## 다음 작업 후보

- certificate-02.pdf가 준비되면 `src/data/resume.ts`의 수료증 2 항목에 `fileUrl`을 추가한다.
- 실제 이메일, 교육기관명, 자격증 정보가 확정되면 데이터 파일에서만 교체한다.
