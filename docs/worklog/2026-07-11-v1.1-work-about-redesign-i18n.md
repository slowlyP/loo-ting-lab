# v1.1 Work / About / Project Detail Redesign + KO/EN Worklog

## Summary

The portfolio moved from the dark Lab Browser presentation to a bright Work Gallery. The previous concept was distinctive, but its OS/HUD framing competed with project content. The new design prioritizes project context, contribution boundaries, evidence, typography, and reading flow.

## Work Gallery

- Replaced the compact three-column archive with a responsive two-column gallery.
- Added CSS-based abstract project thumbnails because verified screenshots are not available for every project.
- Improved card hierarchy for category, status, title, subtitle, summary, technology, and the case-study action.
- Made `/` render the same selected-work experience while retaining the existing route.

## About Me

- Reduced the page to a concise introduction, areas of interest, working process, and contact links.
- Preserved the existing GitHub, email, and resume PDF access.

## Project Detail

- Reorganized the existing content into an editorial case study with overview, role, highlights, technology, structure, status, experiments, outcomes, resources, media, and repository access.
- Retained optional sections for dataset labels, classifier structure, experiment logs, and metrics.
- Kept all existing external links and original project data.

## KO/EN architecture

- `LanguageContext.tsx` owns state, persistence, document language, and Korean UI fallback.
- `useLanguage.ts` contains the shared context/hook and storage key.
- `translations.ts` contains common UI, Work, About, Detail, and footer copy.
- `projectTranslations.ts` stores English project copy by original project ID and merges it over unchanged source projects.
- The localStorage key is `loo-ting-lab-language`; only `en` is accepted as an alternative, so missing or invalid values resolve to `ko`.

## Translation validation

- English copy translates or condenses existing confirmed content.
- No new technology, role, result, metric, or ownership claim was introduced.
- STACCATO and 404 R·N·F AI remain explicitly framed around team participation, review, architecture understanding, and integration flow.
- Wizard Defense remains a personal playable prototype rather than a released product.
- Inquiry Dataset metrics retain their dataset-specific limitations.

## Network background

- Added one global canvas layer using `requestAnimationFrame` and window pointer events.
- Applied `pointer-events: none`, low-opacity lines and dots, a 1.75 DPR cap, fewer mobile points, cleanup handlers, and static reduced-motion rendering.
- No animation or canvas library was installed.

## Files intentionally unchanged

- `src/data/projects.ts`
- `src/data/resume.ts`
- `public/assets/resume/documents/resume.pdf`
- `vite.config.ts`
- `.github/workflows`
- `package.json`
- `package-lock.json`

## Verification

- `npm.cmd run lint`: passed.
- `npm.cmd run build`: passed.
- Automated browser validation was unavailable in this session; a manual visual pass is recommended.

## Remaining item

- Review desktop and mobile rendering in a browser before merging, with attention to long English copy and canvas intensity.
