# v1.4.2 Animal Pang Hero Video Fit Worklog

## Summary

Adjusted the Animal Pang Hero video to preserve the complete portrait mobile UI instead of cropping it into the shared landscape frame.

## Implementation

- Added an optional `cover | contain` fit value to project video metadata.
- Set only Animal Pang to `contain`.
- Kept projects without a fit value on the existing `cover` behavior.
- Used a black video background so letterbox or pillarbox space remains intentional.

## Preserved behavior

- Controls, poster, metadata preload, inline playback, and Hero video error fallback remain.
- Autoplay and loop remain absent.
- Random Wizard Defense video and gallery behavior remain unchanged.
- AppInToss upcoming-release wording and unrelated project content remain unchanged.
- No dependency or protected-file change was made.

## Verification

- Lint: passed (`npm.cmd run lint`)
- Build: passed (`npm.cmd run build`, 110 modules transformed)
- Diff check: passed (`git diff --check`)
- Hero `autoPlay | autoplay | loop` search: no matches
- Static fit review: Animal Pang uses `object-contain`; Wizard Defense keeps the `object-cover` default
- Manual review: not run because no browser surface was available

## Remaining issues

- Complete a manual full-frame and theme review when a browser surface is available.
