# v1.4.2 Release Notes

## Animal Pang Hero Video Fit

- Added optional per-project Hero video fit metadata.
- Set the portrait-oriented Animal Pang demo to `contain` so the complete mobile UI remains visible.
- Kept a black background around any letterbox or pillarbox space.
- Preserved the preview poster, controls, metadata preload, inline playback, and error fallback.

## Preserved behavior

- Random Wizard Defense retains the existing default `cover` presentation.
- No autoplay or loop was added.
- AppInToss remains an upcoming release / release-preparation target.
- Existing gallery, project content, dependencies, and protected files remain unchanged.

## Verification

- Lint: passed
- Build: passed
- Diff check: passed
- Hero autoplay/loop search: no matches
- Manual browser review remains pending because no browser surface was available
