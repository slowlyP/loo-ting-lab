# v1.4.4 Replace Loo Ting Lab with Guild Director Validation

## Before work checklist

- [x] Read workflow and v1.4.3 continuity documents.
- [x] Fetched origin and confirmed the clean `522117e` baseline.
- [x] Confirmed `wizard-defense` and `inquiry-dataset` remained removed.
- [x] Reviewed project data, localization, grouping, previews, and missing-project handling.

## After work checklist

- [x] Replaced the Loo Ting Lab project record with `guild-director`.
- [x] Added synchronized Korean and English prototype content.
- [x] Connected only the supplied GitHub URL.
- [x] Added an abstract preview registration and no media registration.
- [x] Preserved site branding, retained projects, assets, and protected files.

## Command results

- `npm.cmd run lint`: passed (ESLint, exit code 0)
- `npm.cmd run build`: passed (`tsc -b && vite build`, 110 modules transformed, exit code 0)
- `git diff --check`: passed (exit code 0; line-ending conversion warnings only)

## Page and routing checks

- Project data contains four records. Team order is STACCATO then 404 R·N·F AI; Personal order is Stack Stack Animal Pang then Project Guild Director.
- `guild-director` has Korean source data, an English overlay, and the exact GridTacticsPrototype GitHub URL.
- `loo-ting-lab`, `wizard-defense`, and `inquiry-dataset` have no current project registration and therefore use the existing missing-project view.
- `guild-director` uses an abstract preview record and has no image path, service field, project video, or gallery registration.
- Theme and language infrastructure was unchanged; both Korean and English project data passed TypeScript/build validation.
- Manual browser review was unavailable because the local server returned HTTP 200 but no browser was attached to this session.

## Remaining issues

- Complete the requested visual/click review when an in-app browser is available.

## Merge assessment

Automated validation and static data/route review pass. The patch is suitable for manual UI review before merge; browser rendering remains the only unverified item.
