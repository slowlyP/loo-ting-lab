# v1.4.3 Remove Wizard Defense and Inquiry Validation

## Before work checklist

- [x] Read repository workflow and continuity documents.
- [x] Fetched origin and confirmed HEAD, `origin/dev`, and `origin/main` at `94b04f5`.
- [x] Confirmed a clean initial worktree on the requested branch.
- [x] Reviewed project registries, grouping, and missing-project routing.

## After work checklist

- [x] Removed `wizard-defense` and `inquiry-dataset` from current project exposure.
- [x] Preserved the four retained project records and requested group order.
- [x] Preserved removed-project asset files and historical documents.
- [x] Preserved protected files and dependencies.

## Command results

- `npm.cmd run lint`: passed (ESLint, exit code 0)
- `npm.cmd run build`: passed (`tsc -b && vite build`, 110 modules transformed, exit code 0)
- `git diff --check`: passed (exit code 0; line-ending conversion warnings only)

## Page and routing checks

- Work project data: four records only; Team order is STACCATO then 404 R·N·F AI, and Personal order is Stack Stack Animal Pang then Loo Ting Lab.
- Removed direct routes: static route review confirms both IDs fail `getLocalizedProject` and use the existing missing-project view.
- Retained routes: all four IDs remain registered; build and TypeScript validation passed.
- KO/EN: all four IDs remain in Korean source data and English localization data.
- Light/dark: shared theme implementation was unchanged.
- Manual browser review: unavailable because the local server responded successfully but no browser was attached to this session.

## Remaining issues

- Complete the requested visual/click review when an in-app browser is available.

## Merge assessment

Automated validation and static route/data review pass. The patch is suitable for manual UI review before merge; browser rendering remains the only unverified item.
