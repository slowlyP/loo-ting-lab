# v1.2 Dark Mode + Network Background Tuning Validation

## Before work

- Branch: `feature/v1.2-dark-mode-network-tuning`.
- Start commit: `835f450`, tagged `v1.1.0`.
- Starting worktree: clean.
- No pre-existing theme context, theme storage, or root theme attribute was present.

## Theme structure

- Added the typed `light | dark` ThemeContext, useTheme hook, and ThemeProvider with Fast Refresh-safe file separation.
- Default is light. Only the exact stored value `dark` selects dark mode; missing or invalid values resolve to light.
- Storage key: `loo-ting-lab-theme`.
- ThemeProvider writes `data-theme` and `style.colorScheme` to `document.documentElement` in a layout effect.
- ThemeProvider wraps LanguageProvider, but theme and language use separate contexts and storage keys.
- ThemeToggle uses existing react-icons and localized KO/EN aria-label/title text.

## Visual scope

- Added dark styles for App, Header, Footer, LanguageToggle, ThemeToggle, Work headings and group separators, ProjectCard, ProjectThumbnail abstract states, TechStackBadges, About, and all Project Detail sections.
- Actual preview images keep their original brightness; their overlaid titles use white text and a dark shadow for contrast.
- Dark cards use translucent navy/slate surfaces rather than black.
- Resume and Contact direct routes retain their pre-existing navy panels and light text, while the shared page background, Header, and Footer now follow the selected theme.

## Network tuning

- Point counts remain 58–72 desktop and 26–34 mobile.
- Connection distance remains 148px; pointer radius remains 170px.
- Light point radii: 1.65 / 2.15px; opacity: 0.25 / 0.30.
- Dark point radii: 1.75 / 2.25px; opacity: 0.30 / 0.36.
- Line width: 0.75px; light maximum opacity: 0.17; dark maximum opacity: 0.20.
- Light triangle opacity: 0.032 / 0.038; dark: 0.038 / 0.046.
- Theme changes rerun the canvas effect with the selected palette.
- Mobile frame limiting, DPR 1.75 cap, requestAnimationFrame, pointer-events none, reduced-motion static rendering, and cleanup remain intact.

## Regression checks

- KO/EN storage and translations remain independent from theme state.
- Project preview metadata/images, live-site URLs, ownership grouping, route structure, project facts, technology data, and 404 presentation artifact remain unchanged.
- No dependency was installed.
- Package manifests, Vite/GitHub Pages/workflows, and Resume data/PDF/links have no diff.

## Interaction verification

- Theme toggle state, storage writes, root attribute, colorScheme, default/fallback, and independent context behavior were verified from the typed implementation and production build.
- An in-app browser was requested for visual and click testing, but no browser was available in the session. Manual desktop/mobile verification remains recommended before merge.

## Commands

- `npm.cmd run lint`: passed.
- `npm.cmd run build`: passed; Vite transformed 107 modules.
- `git diff --check`: passed.

## Merge assessment

- Static, lint, TypeScript, production build, protected-file, and structural checks pass.
- Safe to prepare for review after the recommended manual visual pass.
