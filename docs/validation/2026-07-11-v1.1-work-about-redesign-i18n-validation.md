# v1.1 Work / About / Project Detail + KO/EN Validation

## Before work

- Branch confirmed: `feature/v1.1-work-about-redesign-i18n`.
- HEAD, `origin/dev`, and `origin/main` were all `e6d9b84` after `git fetch origin --prune`.
- Starting worktree was clean.
- AGENTS, workflow, version plan, checklist, and latest request/validation/worklog/release records were reviewed.

## Implementation checks

- HashRouter and all existing route strings remain present: `/`, `/projects`, four `/projects/:projectId` targets, `/about`, `/resume`, and `/contact`.
- Korean is the default and the only accepted stored alternative is `en`; invalid values therefore resolve to `ko`.
- `loo-ting-lab-language` is written on language changes and read during initial state creation.
- UI translation lookup falls back to the Korean tree.
- Project translations are keyed by the original project ID and merge into the unchanged source data.
- Technology names, project names, metrics, URLs, paths, and project links remain sourced from `projects.ts`.
- STACCATO and 404 R·N·F AI English copy retains team participation, review, architecture understanding, and integration-flow wording.
- No unverified technologies, solo ownership, or new performance claims were added.
- CSS abstract thumbnails are used because verified project screenshots are not available for the gallery.
- The network canvas is global, non-interactive, low-opacity, DPR-capped, mobile-limited, and static under reduced motion.

## Route and interaction verification

- Route declarations and production compilation were verified for `/`, `/projects`, `/projects/:projectId`, `/about`, `/resume`, and `/contact`.
- All four expected project IDs remain in the source data: `staccato`, `wizard-defense`, `inquiry-dataset`, and `ai-accident-detection`.
- KO → EN → KO control behavior is implemented with explicit buttons and `aria-pressed` state.
- Refresh persistence and invalid-value fallback were verified from the state initializer and storage effect.
- Long copy uses responsive grids, `min-w-0`, `break-words`, flexible wrapping, and mobile-first spacing.
- Automated visual/click verification could not run because no in-app browser was available in the session. A manual browser pass remains recommended before merge, especially for canvas visual intensity and exact mobile wrapping.

## Link and protected-file checks

- `src/data/projects.ts` changes are limited to the approved STACCATO and 404 R·N·F AI live service URLs; GitHub, YouTube, artifact PDF, facts, and v1.0.1 technology stacks remain unchanged.
- `src/data/resume.ts`, `public/assets/resume/documents/resume.pdf`, and existing resume URLs have no diff.
- `package.json`, `package-lock.json`, `vite.config.ts`, and `.github/workflows` have no diff.
- No new dependency was installed.

## Commands

- `npm.cmd run lint`: passed.
- `npm.cmd run build`: passed.
- Vite 8.1.3 production build completed with 102 transformed modules.

## Project card hover preview follow-up

- Added `src/data/projectPreviews.ts` so preview assets remain separate from factual project and translation data.
- All projects currently use the CSS abstract fallback because verified image and MP4 assets are not present.
- Image load errors, video load errors, and rejected video playback fall back to the abstract thumbnail instead of displaying broken media.
- A future image or MP4 under `public/assets/projects/...` can be enabled by changing only the matching preview entry.
- Video previews render only when `kind` is `video` and use `muted`, `loop`, `playsInline`, and `preload="metadata"`.
- Playback is limited to devices matching `(hover: hover) and (pointer: fine)`; touch/mobile devices retain a stable static thumbnail.
- Reduced-motion users keep the preview readable without scaling or translation motion, and video autoplay is skipped.
- Follow-up verification: `npm.cmd run lint`, `npm.cmd run build`, and `git diff --check` passed.

## Preview assets and live services follow-up

- Confirmed and loaded `public/assets/projects/staccato/preview.png` and connected it as the STACCATO image preview.
- Confirmed and loaded `public/assets/projects/ai-accident-detection/preview.png` and connected it as the 404 R·N·F AI image preview.
- Wizard Defense and Inquiry Dataset remain on the abstract fallback.
- Added the STACCATO live service URL: `https://mbc-sw.iptime.org:3221/`.
- Replaced the 404 `links.service` presentation URL with the live service URL: `https://404-rnf.ddoriny.com/`.
- The 404 presentation PDF remains unchanged in `detail.artifacts`.
- Project Detail displays a localized live-site action: `서비스 열기` / `Open Live Site`.
- BASE_URL path resolution and image-load fallback to the abstract thumbnail remain active.
- Asset/service follow-up verification: `npm.cmd run lint`, `npm.cmd run build`, and `git diff --check` passed.

## Project ownership grouping follow-up

- Added a separate `projectOwnership.ts` registry instead of changing project descriptions or technology data.
- Team Projects order is fixed to STACCATO followed by 404 R·N·F AI, producing the same two-card row at the `md` two-column breakpoint.
- Personal Projects order is fixed to Wizard Defense followed by Inquiry Dataset.
- Cards display localized ownership pills: `팀 프로젝트`, `개인 프로젝트`, `개인 데이터 프로젝트` and their English equivalents.
- Project Detail displays the same localized ownership as `Project Type`.
- STACCATO and 404 cards expose `서비스 열기` / `Open Live Site`; projects without service URLs do not render the action.
- The service action is a separate external anchor beside the internal case-study Link, so clicks do not bubble through an enclosing card link.
- Existing preview images, project status pills, detail service actions, and the 404 presentation PDF artifact remain in place.
- Ownership follow-up verification: `npm.cmd run lint`, `npm.cmd run build`, and `git diff --check` passed.

## Remaining issue

- Complete a manual desktop/mobile browser pass when a browser surface is available.

## Merge assessment

- Static checks, TypeScript production build, lint, protected-file checks, factual-scope review, and route declarations pass.
- Safe to prepare for review after the recommended manual visual pass.
