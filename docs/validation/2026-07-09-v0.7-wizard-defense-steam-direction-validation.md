# v0.7 Wizard Defense PC / Steam Direction Validation

## 작업 전 체크리스트

- [x] AGENTS.md 확인
- [x] CLAUDE.md 확인
- [x] docs/WORKFLOW.md 확인
- [x] docs/VERSION_PLAN.md 확인
- [x] docs/CHECKLIST.md 확인
- [x] 최신 change-request / validation / worklog / release 문서 확인
- [x] git status 확인
- [x] 현재 브랜치 확인
- [x] git fetch origin 실행
- [x] origin/dev와 현재 브랜치 차이 확인
- [x] Wizard Defense Notion 링크 접근 및 최신 방향성 확인

## 작업 후 체크리스트

- [x] Wizard Defense 현재 노출 문구를 PC / Steam 지향으로 수정
- [x] `Mobile Landscape`를 `PC / Steam Direction`으로 수정
- [x] 모바일 가로형 / 모바일 가로 화면 기준 표현을 현재 노출 데이터에서 제거
- [x] Steam 출시 완료처럼 보이는 표현을 사용하지 않음
- [x] Steam 링크는 확인되지 않아 추가 예정으로 표시
- [x] v0.4 과거 기록 문서는 직접 수정하지 않음
- [x] v0.7 문서에 v0.4 당시 표현을 최신 Notion 기준으로 바꾼 사실을 기록
- [x] docs/VERSION_PLAN.md 수정

## lint 결과

- `npm.cmd run lint` 통과.

## build 결과

- `npm.cmd run build` 통과.
- `tsc -b && vite build` 완료.

## 페이지 / 라우팅 확인 결과

- 코드 레벨에서 `src/data/projects.ts`의 Wizard Defense 데이터와 기존 ProjectDetail 렌더링 구조를 확인했다.
- 브라우저 수동 확인은 별도로 수행하지 않았다.

## 모바일 표현 재검색 결과

- `rg -n -i "모바일|mobile|Mobile Landscape|가로형|가로 화면" src docs/VERSION_PLAN.md` 실행.
- 현재 화면 노출 소스와 VERSION_PLAN.md에서는 검색 결과 없음.
- v0.7 change-request / validation / worklog / release 문서에는 변경 이력과 금지 표현 검증 맥락으로만 관련 표현이 남아 있음.

## 남은 문제

- 확인된 Steam 페이지, 데모 URL, 영상, 스크린샷은 아직 없어 추가 예정으로 표시했다.
- Wizard Defense GitHub 저장소 URL은 확인되지 않아 빈 값으로 유지했다.

## dev에 병합해도 안전한지 여부

- lint와 build가 통과했으므로 dev 병합 전 리뷰 가능한 상태다.
